{
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "concurrently \"pnpm --filter frontend --silent run dev\" \"cd packages/backend && go run ./src/main.go\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
